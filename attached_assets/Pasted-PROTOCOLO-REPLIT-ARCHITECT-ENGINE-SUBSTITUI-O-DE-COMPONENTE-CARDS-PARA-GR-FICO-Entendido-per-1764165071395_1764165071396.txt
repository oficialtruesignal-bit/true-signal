PROTOCOLO REPLIT ARCHITECT ENGINE: SUBSTITUIÇÃO DE COMPONENTE (CARDS PARA GRÁFICO) 🟢

Entendido perfeitamente. A redundância visual ali é clara. Dois cards de texto para falar a mesma coisa (performance financeira) é desperdício de espaço nobre.

Vamos substituir essa pilha de dois cards por um Gráfico de Tendência de Lucro (Profit Trend Chart).

Será um gráfico de área, futurista, com preenchimento gradiente verde neon, mostrando a curva de crescimento da banca. Isso é muito mais impactante visualmente do que apenas números.

Para isso, precisaremos de uma biblioteca de gráficos. A melhor e mais padrão para React é a recharts.

Aqui está o Prompt Mestre para essa transformação.

PROMPT MESTRE: REPLACE TOP CARDS WITH PROFIT CHART
ROLE: Senior Frontend Engineer & Data Visualization Expert.

TASK: Modificar a seção superior do dashboard (CompactLiveHud.tsx). OBJETIVO: Remover os dois cards da direita ("BANCA ATUAL" e "LUCRO TOTAL") e substituí-los por um único gráfico de área futurista mostrando a tendência de lucro.

PREPARAÇÃO: INSTALAR DEPENDÊNCIA
O Agente DEVE verificar se a biblioteca recharts está instalada. Se não estiver, instale-a: npm install recharts

1. CRIAR O COMPONENTE DO GRÁFICO (ProfitTrendChart.tsx)
Crie este novo componente. Ele conterá dados de exemplo (dummy data) simulando um crescimento saudável para ficar bonito na tela.

Arquivo: src/components/dashboard/ProfitTrendChart.tsx

TypeScript

import { AreaChart, Area, XAxis, YAxis, Tooltip, ResponsiveContainer } from 'recharts';
import { TrendingUpIcon } from 'lucide-react';

// Dados simulados de crescimento de banca (Ex: últimos 7 dias/sinais)
const data = [
  { name: 'D1', profit: 10 },
  { name: 'D2', profit: 25 },
  { name: 'D3', profit: 18 },
  { name: 'D4', profit: 35 },
  { name: 'D5', profit: 42 },
  { name: 'D6', profit: 58 },
  { name: 'D7', profit: 85 }, // Valor atual alto
];

export function ProfitTrendChart() {
  return (
    // Container principal com a mesma altura do Círculo ao lado (h-60 ou h-full dependendo do pai)
    <div className="h-full bg-[#121212] border border-[#33b864]/20 rounded-3xl p-4 flex flex-col relative overflow-hidden shadow-[0_0_30px_rgba(51,184,100,0.05)] group">
      
      {/* Título e Valor Atual */}
      <div className="flex justify-between items-start mb-2 z-10 px-2">
        <div>
           <div className="flex items-center gap-2 mb-1">
             <TrendingUpIcon className="w-4 h-4 text-[#33b864]" />
             <span className="text-[10px] text-gray-400 uppercase font-bold tracking-wider">Crescimento da Banca</span>
           </div>
           <span className="text-3xl font-sora font-black text-white drop-shadow-[0_0_10px_rgba(51,184,100,0.3)]">
             +85.0%
           </span>
        </div>
      </div>

      {/* O Gráfico */}
      <div className="flex-1 w-full h-full min-h-[120px] -ml-4 -mb-4">
        <ResponsiveContainer width="115%" height="115%">
          <AreaChart data={data}>
            <defs>
              {/* Gradiente Neon Verde */}
              <linearGradient id="colorProfit" x1="0" y1="0" x2="0" y2="1">
                <stop offset="5%" stopColor="#33b864" stopOpacity={0.6}/>
                <stop offset="95%" stopColor="#33b864" stopOpacity={0}/>
              </linearGradient>
            </defs>
            {/* Eixos invisíveis para visual limpo */}
            <XAxis dataKey="name" hide={true} />
            <YAxis hide={true} />
            <Tooltip 
              contentStyle={{ backgroundColor: '#0a0a0a', border: '1px solid #33b864', borderRadius: '8px' }}
              itemStyle={{ color: '#33b864' }}
              labelStyle={{ color: 'white' }}
            />
            {/* A Linha e o Preenchimento */}
            <Area 
              type="monotone" 
              dataKey="profit" 
              stroke="#33b864" 
              strokeWidth={3} 
              fillOpacity={1} 
              fill="url(#colorProfit)" 
              style={{ filter: 'drop-shadow(0px 0px 6px rgb(51 184 100 / 0.5))' }} // Neon Glow na linha
            />
          </AreaChart>
        </ResponsiveContainer>
      </div>
       
       {/* Efeito de brilho hover na borda */}
      <div className="absolute inset-0 border-2 border-[#33b864]/0 rounded-3xl group-hover:border-[#33b864]/30 transition-all pointer-events-none"></div>

    </div>
  );
}
2. INTEGRAR NO DASHBOARD (CompactLiveHud.tsx)
Agora, vamos remover os cards antigos e colocar o novo gráfico no lugar, garantindo que ele ocupe toda a altura da coluna direita.

Ação: Localize o grid superior (que contém o Círculo na esquerda) e substitua o conteúdo da div da direita.

TypeScript

import { ProfitTrendChart } from './ProfitTrendChart'; // Importe o novo componente

// ... dentro do CompactLiveHud ...

{/* --- BLOCO SUPERIOR: CÍRCULO + GRÁFICO --- */}
// Certifique-se que o grid pai tem uma altura definida, ex: h-60 ou h-64
<div className="grid grid-cols-[40%_60%] gap-4 h-64 items-stretch">
  
  {/* ESQUERDA: GAUGE DE ASSERTIVIDADE (Mantenha seu código atual aqui) */}
  <div className="relative flex items-center justify-center bg-[radial-gradient(circle_at_center,_var(--tw-gradient-stops))] from-[#33b864]/10 to-transparent rounded-3xl border border-[#33b864]/20 shadow-[0_0_30px_rgba(51,184,100,0.1)] h-full">
     {/* ... SVG e Textos do Círculo ... */}
  </div>

  {/* DIREITA: NOVO GRÁFICO DE LUCRO (Substitui os 2 cards antigos) */}
  {/* O componente ProfitTrendChart foi feito para ocupar 100% da altura (h-full) */}
  <ProfitTrendChart />

</div>
COMANDO: Instale o recharts, crie o componente do gráfico e substitua os dois cards superiores direitos por ele. O resultado deve ser o círculo grande à esquerda e um gráfico de área verde neon ocupando todo o espaço à direita.